from utils.math_util import add, average
from utils.random_util import random_name
from utils.time_util import get_now

print("当前时间：", get_now())
print("随机用户：", random_name())
print("平均分：", average([80, 90, 70]))
